/**
 * Application constants
 * Centralizes hardcoded values for easier maintenance
 */

/**
 * Default user ID for MVP phase
 * TODO: Replace with actual authentication in Phase 3 (multi-user support)
 * This can be overridden via environment variable in the future
 */
export const DEFAULT_USER_ID = import.meta.env.VITE_DEFAULT_USER_ID || 'default-user'

